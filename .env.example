# Auth.js / NextAuth secret — generate with: openssl rand -hex 32
AUTH_SECRET=
AUTH_TRUST_HOST=true
NEXTAUTH_URL=http://localhost:3000

# AES-256-GCM key for encrypting sensitive DB fields — generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# Salesforce Integration (OAuth flow — users authenticate via popup)
SF_LOGIN_URL=https://login.salesforce.com
SF_CLIENT_ID=
SF_CLIENT_SECRET=
